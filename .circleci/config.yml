version: 2.1

executors:
  my-executor:
    docker:
      - image:  circleci/node:13.8.0

jobs:
  main_commit:
    executor: my-executor
    steps:
      - run:
          command:  |
            echo "$CIRCLECI_WORKFLOW_ID"

workflows:
  commit:
    jobs:
      - main_commit