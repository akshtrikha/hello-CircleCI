version: 2.1

executors:
  aws-cli:
    docker:
      - image:  amazon/aws-cli

jobs:
  test-checkout:
    executor: aws-cli
    steps:
      - checkout
      - run:
          command:  |
            ls -a

workflows:
  commit:
    jobs:
      - test-checkout