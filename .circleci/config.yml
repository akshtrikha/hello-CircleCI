version: 2.1

executors:
  aws-cli:
    docker:
      - image:  amazon/aws-cli

jobs:
  test-checkout:
    executor: aws-cli
    working_directory: ~/source-code
    steps:
      - checkout
      - run:
          command:  |
            pwd
            ls -a
            cd ~/source-code && ls -a

workflows:
  tests:
    jobs:
      - test-checkout